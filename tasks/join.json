{
  "description": "Join nodes to an AD domain using realm join",
  "parameters": {
    "server": {
      "description": "IPA server",
      "type": "Simplib::Host"
    },
    "stdin_password": {
      "description": "Password for authorization to the IPA server",
      "type": "Optional[String]"
    },
    "options": {
      "description": "Other command line options from `realm join`, specified as an argument string",
      "type": "Optional[String]"
    }
  },
  "files": ["ruby_task_helper/files/task_helper.rb"]
}
