{
  "description": "Join nodes to an AD domain using realm join",
  "parameters": {
    "server": {
      "description": "The domain controlling AD server that the hosts will be joined to",
      "type": "String"
    },
    "stdin_password": {
      "description": "Password used for authorization to the AD server",
      "type": "Optional[String]"
    },
    "options": {
      "description": "Other command line options from `realm join`, specified as an argument string. For example: `--verbose`.",
      "type": "Optional[String]"
    }
  },
  "files": ["ruby_task_helper/files/task_helper.rb"]
}
